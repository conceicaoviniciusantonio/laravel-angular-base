services:
  db:
    image: mysql:8.0
    container_name: connector_manager_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: connector_manager
      MYSQL_USER: app_user
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: root_secret
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot_secret"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - connector_net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: connector_manager_backend
    restart: unless-stopped
    volumes:
      - ./backend:/var/www/backend
    ports:
      - "8000:8000"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: connector_manager
      DB_USERNAME: app_user
      DB_PASSWORD: secret
    depends_on:
      db:
        condition: service_healthy
    networks:
      - connector_net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: connector_manager_frontend
    restart: unless-stopped
    volumes:
      - ./frontend:/var/www/frontend
    ports:
      - "4200:4200"
    networks:
      - connector_net

volumes:
  db_data:

networks:
  connector_net:
    driver: bridge
